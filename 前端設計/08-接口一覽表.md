# 熊媽媽法庭 - 接口一覽表

**項目名稱**：熊媽媽法庭（Mother Bear Court）  
**文檔版本**：v1.0  
**創建日期**：2024年  
**最後更新**：2024年

---

## 📋 接口列表

### Session管理接口（快速體驗模式）

| 接口 | 方法 | 應用頁面 | 應用功能 | 簡單描述 | 開發狀態 |
|------|------|----------|----------|----------|----------|
| `/api/v1/sessions/create` | GET | `/quick-experience/create` | 創建Session | 創建快速體驗模式的Session ID | ⏳ 待開發 |

---

### 認證相關接口

| 接口 | 方法 | 應用頁面 | 應用功能 | 簡單描述 | 開發狀態 |
|------|------|----------|----------|----------|----------|
| `/api/v1/auth/register` | POST | `/auth/register` | 用戶註冊 | 用戶註冊，發送郵箱驗證碼 | ⏳ 待開發 |
| `/api/v1/auth/send-verification-code` | POST | `/auth/register` | 發送驗證碼 | 發送郵箱驗證碼 | ⏳ 待開發 |
| `/api/v1/auth/verify-email` | POST | `/auth/register` | 驗證郵箱 | 驗證郵箱驗證碼 | ⏳ 待開發 |
| `/api/v1/auth/login` | POST | `/auth/login` | 用戶登錄 | 郵箱+密碼登錄，返回Token | ⏳ 待開發 |
| `/api/v1/auth/reset-password` | POST | `/auth/forgot-password` | 發送重置密碼郵件 | 發送重置密碼驗證碼到郵箱 | ⏳ 待開發 |
| `/api/v1/auth/reset-password-confirm` | POST | `/auth/forgot-password` | 確認重置密碼 | 驗證碼驗證後設置新密碼 | ⏳ 待開發 |

---

### 用戶相關接口

| 接口 | 方法 | 應用頁面 | 應用功能 | 簡單描述 | 開發狀態 |
|------|------|----------|----------|----------|----------|
| `/api/v1/user/profile` | GET | `/profile/index` | 獲取用戶資料 | 獲取當前登錄用戶的詳細資料 | ⏳ 待開發 |
| `/api/v1/user/profile` | PUT | `/profile/index` | 更新用戶資料 | 更新用戶基本信息、頭像、偏好設置 | ⏳ 待開發 |

---

### 配對相關接口

| 接口 | 方法 | 應用頁面 | 應用功能 | 簡單描述 | 開發狀態 |
|------|------|----------|----------|----------|----------|
| `/api/v1/pairing/create` | POST | `/profile/pairing` | 創建配對邀請 | 生成配對邀請碼，等待對方加入 | ⏳ 待開發 |
| `/api/v1/pairing/join` | POST | `/profile/pairing` | 加入配對 | 通過邀請碼加入配對 | ⏳ 待開發 |
| `/api/v1/pairing/status` | GET | `/profile/pairing` | 獲取配對狀態 | 獲取當前配對狀態（待配對/已配對/已取消） | ⏳ 待開發 |

---

### 案件相關接口

| 接口 | 方法 | 應用頁面 | 應用功能 | 簡單描述 | 開發狀態 |
|------|------|----------|----------|----------|----------|
| `/api/v1/cases` | POST | `/quick-experience/create`<br>`/case/create` | 創建案件 | 創建新案件（快速體驗模式或完整模式） | ⏳ 待開發 |
| `/api/v1/cases` | GET | `/case/list` | 獲取案件列表 | 獲取用戶的案件列表，支持篩選和排序 | ⏳ 待開發 |
| `/api/v1/cases/:id` | GET | `/case/:id`<br>`/quick-experience/result/:id` | 獲取案件詳情 | 獲取案件詳細信息 | ⏳ 待開發 |
| `/api/v1/cases/:id` | PUT | `/case/:id` | 更新案件 | 更新案件信息（僅草稿狀態可更新） | ⏳ 待開發 |
| `/api/v1/cases/:id/submit` | POST | `/case/:id`<br>`/quick-experience/create` | 提交案件 | 提交案件，開始AI審理流程 | ⏳ 待開發 |
| `/api/v1/cases/:id/evidence` | POST | `/quick-experience/create`<br>`/case/create` | 上傳證據 | 上傳案件證據（圖片或視頻） | ⏳ 待開發 |

---

### 判決相關接口

| 接口 | 方法 | 應用頁面 | 應用功能 | 簡單描述 | 開發狀態 |
|------|------|----------|----------|----------|----------|
| `/api/v1/cases/:id/judgment` | POST | `/case/:id/review` | 生成判決 | 觸發AI生成判決（案件提交後自動觸發） | ⏳ 待開發 |
| `/api/v1/judgments/:id` | GET | `/judgment/:id`<br>`/judgment/:id/result`<br>`/quick-experience/result/:id` | 獲取判決詳情 | 獲取判決書完整內容（問題分析、判決結果、責任分比例、具體建議） | ⏳ 待開發 |
| `/api/v1/judgments/:id/accept` | POST | `/judgment/:id/result` | 接受/拒絕判決 | 用戶接受或拒絕判決，可選評分（1-5星） | ⏳ 待開發 |

---

### 和好方案相關接口

| 接口 | 方法 | 應用頁面 | 應用功能 | 簡單描述 | 開發狀態 |
|------|------|----------|----------|----------|----------|
| `/api/v1/judgments/:id/reconciliation-plans` | POST | `/judgment/:id/result` | 生成和好方案 | 基於判決結果生成和好方案列表 | ⏳ 待開發 |
| `/api/v1/judgments/:id/reconciliation-plans` | GET | `/reconciliation/:judgmentId` | 獲取和好方案列表 | 獲取該判決的所有和好方案，支持篩選 | ⏳ 待開發 |
| `/api/v1/reconciliation-plans/:id` | GET | `/reconciliation/:judgmentId/:id` | 獲取方案詳情 | 獲取和好方案詳細內容（活動、溝通練習、親密互動建議） | ⏳ 待開發 |
| `/api/v1/reconciliation-plans/:id/select` | POST | `/reconciliation/:judgmentId/:id` | 選擇和好方案 | 選擇要執行的和好方案 | ⏳ 待開發 |

---

### 執行追蹤相關接口

| 接口 | 方法 | 應用頁面 | 應用功能 | 簡單描述 | 開發狀態 |
|------|------|----------|----------|----------|----------|
| `/api/v1/execution/confirm` | POST | `/reconciliation/:judgmentId/:id` | 確認執行和好方案 | 確認開始執行選定的和好方案 | ⏳ 待開發 |
| `/api/v1/execution/dashboard` | GET | `/execution/dashboard` | 獲取執行看板 | 獲取所有執行中的方案、已完成方案、統計信息 | ⏳ 待開發 |
| `/api/v1/execution/checkin` | POST | `/execution/:planId/checkin` | 執行打卡 | 記錄執行打卡（執行時間、內容、感受、照片） | ⏳ 待開發 |
| `/api/v1/execution/:planId/records` | GET | `/execution/:planId/checkin` | 獲取執行記錄 | 獲取該方案的執行記錄列表 | ⏳ 待開發 |

---

## 📊 接口統計

### 按模塊分類

| 模塊 | 接口數量 | 開發狀態 |
|------|----------|----------|
| Session管理 | 1 | ⏳ 待開發 |
| 認證相關 | 6 | ⏳ 待開發 |
| 用戶相關 | 2 | ⏳ 待開發 |
| 配對相關 | 3 | ⏳ 待開發 |
| 案件相關 | 6 | ⏳ 待開發 |
| 判決相關 | 3 | ⏳ 待開發 |
| 和好方案相關 | 4 | ⏳ 待開發 |
| 執行追蹤相關 | 4 | ⏳ 待開發 |
| **總計** | **28** | **⏳ 待開發** |

### 按頁面分類

| 頁面 | 使用接口數量 | 主要功能 |
|------|--------------|----------|
| `/auth/register` | 2 | 用戶註冊、郵箱驗證 |
| `/auth/login` | 1 | 用戶登錄 |
| `/auth/forgot-password` | 2 | 重置密碼 |
| `/profile/index` | 2 | 查看和編輯個人資料 |
| `/profile/pairing` | 3 | 配對管理 |
| `/quick-experience/create` | 3 | 快速體驗創建案件 |
| `/quick-experience/result/:id` | 2 | 快速體驗查看判決 |
| `/case/list` | 1 | 案件列表 |
| `/case/create` | 2 | 創建案件（完整模式） |
| `/case/:id` | 3 | 案件詳情、更新、提交 |
| `/case/:id/review` | 1 | 審理中頁面 |
| `/judgment/:id` | 1 | 判決詳情 |
| `/judgment/:id/result` | 3 | 判決結果、接受判決、生成和好方案 |
| `/reconciliation/:judgmentId` | 1 | 和好方案列表 |
| `/reconciliation/:judgmentId/:id` | 2 | 方案詳情、選擇方案 |
| `/execution/dashboard` | 1 | 執行看板 |
| `/execution/:planId/checkin` | 2 | 執行打卡 |

---

## 🔑 接口優先級

### P0優先級（MVP核心功能）

**認證相關**：
- ✅ `POST /auth/register` - 用戶註冊
- ✅ `POST /auth/verify-email` - 驗證郵箱
- ✅ `POST /auth/login` - 用戶登錄

**案件相關**：
- ✅ `POST /cases` - 創建案件（支持快速體驗模式）
- ✅ `GET /cases/:id` - 獲取案件詳情
- ✅ `POST /cases/:id/submit` - 提交案件

**判決相關**：
- ✅ `POST /cases/:id/judgment` - 生成判決
- ✅ `GET /cases/:id/judgment` - 獲取判決詳情

### P1優先級（重要功能）

**用戶相關**：
- ⚠️ `GET /user/profile` - 獲取用戶資料
- ⚠️ `PUT /user/profile` - 更新用戶資料

**配對相關**：
- ⚠️ `POST /pairing/create` - 創建配對邀請
- ⚠️ `POST /pairing/join` - 加入配對
- ⚠️ `GET /pairing/status` - 獲取配對狀態

**案件相關**：
- ⚠️ `GET /cases` - 獲取案件列表
- ⚠️ `POST /cases/:id/evidence` - 上傳證據

**判決相關**：
- ⚠️ `POST /judgments/:id/accept` - 接受/拒絕判決

**和好方案相關**：
- ⚠️ `POST /judgments/:id/reconciliation-plans` - 生成和好方案
- ⚠️ `GET /judgments/:id/reconciliation-plans` - 獲取和好方案列表
- ⚠️ `POST /reconciliation-plans/:id/select` - 選擇和好方案

**執行追蹤相關**：
- ⚠️ `POST /execution/confirm` - 確認執行和好方案
- ⚠️ `GET /execution/dashboard` - 獲取執行看板
- ⚠️ `POST /execution/checkin` - 執行打卡

### P2優先級（後續迭代）

**認證相關**：
- ⏸️ `POST /auth/reset-password` - 發送重置密碼郵件
- ⏸️ `POST /auth/reset-password-confirm` - 確認重置密碼

**案件相關**：
- ⏸️ `PUT /cases/:id` - 更新案件

**和好方案相關**：
- ⏸️ `GET /reconciliation-plans/:id` - 獲取方案詳情

**執行追蹤相關**：
- ⏸️ `GET /execution/:planId/records` - 獲取執行記錄

---

## 📝 接口開發狀態說明

### 狀態標識

- **✅ 已完成**：接口已開發完成，已測試，可正常使用
- **🚧 開發中**：接口正在開發中
- **⏳ 待開發**：接口尚未開始開發
- **⚠️ 重要**：重要功能，優先開發
- **⏸️ 暫緩**：後續迭代功能，暫不開發

### 當前狀態

**所有接口目前均為「⏳ 待開發」狀態**

---

## 🔄 接口依賴關係

### 核心流程依賴

**快速體驗流程**：
```
POST /cases (創建案件)
  ↓
POST /cases/:id/submit (提交案件)
  ↓
POST /cases/:id/judgment (生成判決)
  ↓
GET /cases/:id/judgment (獲取判決)
```

**完整模式流程**：
```
POST /auth/register (註冊)
  ↓
POST /auth/verify-email (驗證郵箱)
  ↓
POST /auth/login (登錄)
  ↓
POST /pairing/create (創建配對)
  ↓
POST /pairing/join (加入配對)
  ↓
POST /cases (創建案件)
  ↓
POST /cases/:id/submit (提交案件)
  ↓
POST /cases/:id/judgment (生成判決)
  ↓
GET /cases/:id/judgment (獲取判決)
  ↓
POST /judgments/:id/reconciliation-plans (生成和好方案)
  ↓
POST /reconciliation-plans/:id/select (選擇方案)
  ↓
POST /execution/confirm (確認執行)
  ↓
POST /execution/checkin (執行打卡)
```

---

## 📋 接口文檔規範

### 請求格式

**認證頭**（需要認證的接口）：
```
Authorization: Bearer <token>
```

**Content-Type**：
```
Content-Type: application/json
```

### 響應格式

**成功響應**：
```json
{
  "success": true,
  "data": { ... },
  "message": "操作成功"
}
```

**錯誤響應**：
```json
{
  "success": false,
  "error": {
    "code": "ERROR_CODE",
    "message": "錯誤描述",
    "details": { ... }
  }
}
```

### 錯誤碼

- `400 Bad Request`：請求參數錯誤
- `401 Unauthorized`：未認證或Token過期
- `403 Forbidden`：無權限訪問
- `404 Not Found`：資源不存在
- `422 Unprocessable Entity`：業務邏輯錯誤
- `500 Internal Server Error`：服務器錯誤

---

**文檔版本**：v1.0  
**創建日期**：2024年  
**最後更新**：2024年

